# Copier vers .env.ci après scripts/setup-sonarqube.sh
# NE PAS COMMITTER .env.ci (contient les tokens)

SONAR_TOKEN=remplacer-par-setup-sonarqube-sh
SONAR_ADMIN_PASSWORD=admin

# Git Gitea — scripts/setup-jenkins-git.sh (clone job travel)
GIT_USERNAME=ajoly
GIT_TOKEN=remplacer-par-token-gitea
