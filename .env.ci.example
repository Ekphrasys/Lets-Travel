# Copier vers .env.ci après scripts/setup-sonarqube.sh
# NE PAS COMMITTER .env.ci (contient les tokens)

SONAR_TOKEN=remplacer-par-setup-sonarqube-sh
SONAR_ADMIN_PASSWORD=admin

# Git — scripts/setup-jenkins-git.sh (clone job lets-travel)
GIT_USERNAME=ajoly
GIT_TOKEN=remplacer-par-token-git

# Neo4j — tests d'intégration travel-service
NEO4J_PASSWORD=neo4j_secret
